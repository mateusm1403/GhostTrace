services:
  ghosttrace:
    build: .
    command: python run.py
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      DATABASE_URL: sqlite:///./ghosttrace.db
